import React from 'react';// core foundational library
import ReactDOM from 'react-dom/client';// react-dom is an implementation of react on web, just like react-native is an implementation of react on mobile apps
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
  
);

